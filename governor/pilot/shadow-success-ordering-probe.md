# Shadow SUCCESS ordering probe (Stage A3b-c3/c4)

Disposable probe PR for the `ai-final-review` governance program.

- Purpose: prove the Governor extinguishes a standing success and confirms
  the failure **before** it posts the provider request that makes the old
  evidence non-current — and that every projection is confirmed by an
  independent readback rather than by a PATCH response body.
- The check is **not required**: no ruleset, no branch protection, no
  auto-merge. A green check here governs nothing.
- Merge: **NEVER**. Closed without merge after captures.
- Control plane: `PhysShell/review-governance`,
  branch `experiment/shadow-success-lifecycle`.
