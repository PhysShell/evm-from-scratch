# A5a-c1 external failure domain probe

Used to prove that the edge watchdog revokes a standing success when the
primary dies, and that a dropped webhook delivery is recovered by
reconciliation.

synchronize probe
